# 任务书分析与调研发现

## 任务书硬性要求

1. 目标能力包括：用 AI 原理分析制造环节问题并提出实施方案；选择模型、算力和数据进行分析、模拟或预测；准确表达制造工程问题中的智能技术、设计方法和方案。
2. 选题必须是制造/工业场景，例如设计、生产、质量控制、设备运维、物流仓储、生产管理，且要有真实业务感。可使用实习见闻、企业公开资料、公开工业数据集，不要求真实企业内部数据。
3. 项目需使用课程至少 3 个技术手段，并完成完整 B/S 或 C/S Demo。架构至少包含前端 UI、后端服务/API、数据库、算法模块。
4. 课程强调 vibe coding：提示词工程、上下文工程、规格驱动开发、AI 自动化测试与人工审查、调试和人工接管；git 版本控制是过程要求。
5. 报告必须覆盖背景调研/研究意义、方案设计、数据资源构建、系统实现、测试与集成调试、AI 使用披露、总结展望。答辩为 3 分钟自述 + 7 分钟问答，可能抽查 1-2 段 AI 生成代码。
6. 过程考核 30%，成果考核 50%，答辩 20%。成果部分由设计说明书 25% 和程序 25% 构成，关注架构完整性、至少 3 个技术方向、功能完成度和代码测试质量。

## 候选技术方向映射

首选项目建议采用以下 4 个方向，确保每个方向都有可演示的业务作用：

- 数据预处理与特征工程：缺失值、异常值、类别编码、特征统计和数据质量看板。
- 监督学习分类/回归：质量合格预测、缺陷类别预测或质量风险评分。
- 异常检测/聚类：发现工况偏移、异常批次或未知缺陷模式。
- 可解释分析/可视化：特征重要性、分组对比、趋势图和根因候选排序。

可选增强：时间序列预测、知识库/RAG 问答、规则引擎、阈值告警。增强项只在核心链路稳定后加入。

## 候选选题比较

| 方向 | 数据可得性 | 10天落地性 | 展示效果 | 主要风险 | 判断 |
|---|---:|---:|---:|---|---|
| 质量异常分析与根因辅助诊断 | 高 | 高 | 高 | 根因不能夸大为因果结论 | 首选 |
| 设备预测性维护 | 高 | 中高 | 高 | 时序数据、故障标签和预测定义较难 | 备选1 |
| 生产交期/产能预测与排产辅助 | 中高 | 中 | 高 | 业务约束、优化模型和数据量要求较高 | 备选2 |
| 仓储物流路径与库存预警 | 中高 | 高 | 中高 | 易做成普通管理系统，制造智能性需强化 | 备选3 |
| 视觉表面缺陷检测 | 高 | 中 | 高 | 模型训练、部署和图片标注工作量大 | 不建议作为首选 |

## 首选方向理由

质量异常分析最适合课程约束：公开数据较多，表格型数据清洗和模型训练可控，前端能同时展示数据概览、风险预测、异常批次、特征重要性和诊断建议；算法结果容易做成 3 分钟演示；还能自然覆盖制造质量控制、数据分析、机器学习、异常检测和可解释 AI。

## 选题边界

不要宣称“自动确定真实根因”或“替代质量工程师”。系统定位应为“质量风险预测 + 异常模式发现 + 根因候选辅助”，结论需注明相关性、数据范围和人工复核要求。

## 建议公开数据

- UCI/工业公开数据中的钢板缺陷、制造产品质量、AI4I 2020 Predictive Maintenance 等数据。
- 首选以一个结构化表格数据集为主，必要时补充模拟的工单/批次字段；不同时引入多套难以对齐的数据。
- 数据说明书需记录来源、字段含义、样本量、标签定义、清洗规则、训练/测试划分和许可证/引用信息。

## 第3阶段数据与 AI 记录补充

- 仓库公开数据采用“小样例 + 数据卡 + Schema + 来源索引”的组合，完整下载包和训练数据不进入公开仓库。
- 工艺质量样例统一为 ISO 8601 时间、逗号分隔和小数点格式，便于 Node.js、Python 和浏览器侧复用。
- 计划预测 `% Silica Concentrate`；`% Iron Concentrate` 保留用于说明字段关系，但基线模型默认排除，避免后验质量信息造成目标泄漏。
- `prompt/` 采用按日期、阶段和主题命名的 JSON 文件；记录包含 harness、模型、动作、发现、产物、备份状态和下一步，后续阶段继续追加。
- 本阶段发现本地 `quality-prediction.zip` 不能被标准 ZIP 读取，因此只使用可读取的本地公开镜像抽取样例，并在数据卡中保留复现限制。

## 第3阶段公开发布发现

- 工艺质量原始公开页为 `https://www.kaggle.com/datasets/edumagalhaes/quality-prediction-in-a-mining-process`，Kaggle 页面明确标注 `CC0: Public Domain`。
- 仓库内容应准确表述为“基于 CC0 原始公开数据加工的 10 行教学样例”，同时保留原始来源、许可、抽取行号、转换说明和使用边界。
- 考核规则允许小型数据直接提交 `data/`；因此本阶段使用 GitHub 中的 CSV 作为加工样例公开地址，无需为了 10 行样例强制创建外部数据平台仓库。
- 完整原始文件留在被忽略的 `data/local/`，公开仓库只提交 CSV、Schema、预处理索引和数据卡。
- `process-quality-index.json` 可把原始 SHA-256、抽取行号和输出 SHA-256连成可审计链路，避免样例来源无法复现。
- `% Iron Concentrate` 可能属于后验质量信息，工艺参数基线默认排除该字段；特征重要性只解释关联，不等同于因果关系。
- 第三阶段自动化测试应在断言失败时也关闭 HTTP 服务，否则 Node 测试进程会因开放句柄挂起。

## 阶段 6.1 工艺参数模型启动发现

- `data/local/mining-mirror-a/Book1.csv` 只有 199 条数据记录，可用于预处理和训练流程冒烟验证，不足以代替完整公开数据的正式时间切分评估。
- `data/local/mining-quality-source/quality-prediction.zip` 具有 ZIP 文件头，但缺少中央目录结束记录，属于不完整或截断文件，不能作为训练输入。
- 现有 `cv_tutorial` 环境包含 Python 3.10.20、pandas 2.3.3、scikit-learn 1.7.2、joblib 1.5.3、NumPy 2.2.5 和 ONNX Runtime，但尚未安装 `skl2onnx`。
- 阶段 6.1 继续以 `% Silica Concentrate` 为回归目标，排除 `% Iron Concentrate` 以降低后验质量字段泄漏风险；训练、验证、测试必须按时间顺序划分。
- 训练模型和完整原始数据保留在 `.gitignore` 覆盖的本地路径，公开仓库只提交训练代码、指标摘要、模型清单、接口代码和审计记录。
- 时间外推测试明显比随机切分更严格：当前基线测试 R² 仅为 `0.13100`，但 MAE 相对中位数基线改善 `10.21%`。该结果适合展示可复现基线和部署链路，不应包装成高精度现场模型。
- 全局特征重要性前三项为浮选柱平均气流、胺流量和浮选柱平均液位；它们只能解释模型关联强度，不能直接作为工艺根因结论。
- joblib 与 ONNX 在 128 条独立输入上的最大绝对预测差为 `1.47e-6`，可视为本地部署格式数值一致。
## 阶段 6.1 收尾发现

- 平台工艺预测接口的部署契约是 9 个小时聚合特征，不接收 21 个原始浮选柱字段；原始字段必须先按训练逻辑聚合，否则返回 `invalid_process_features`。
- 工艺模型服务采用显式失败策略：未配置、离线或响应异常时返回 503，不用规则值伪造模型预测；这与视觉链路允许明确标记 fallback 的策略不同。
- 输入范围告警基于训练集各特征最小值/最大值，适合提示分布外输入，但不能代替统计漂移检测或现场上下限。
- 时间外推测试 R² `0.13100` 说明模型解释能力有限；MAE 相对中位数基线改善 `10.21%` 可证明基线有效，但报告必须同时披露低 R² 和单一公开数据源限制。
- 浏览器运行时与 `apply_patch` 同时出现 `helper_unknown_error: setup refresh had errors`，属于本机 Codex 沙箱基础设施问题；代码验收应保留可重复的 HTTP、语法、测试与统一差异证据，并明确未完成截图检查。

## 阶段 6.2 知识库与证据融合发现

- 现有 Node 服务已经提供视觉 `/api/inspect` 和工艺 `/api/process-quality/predict`，最小侵入的接入方式是新增纯决策 API，由调用方提交两类模型结果进行融合。
- 轻量 JSON 知识库适合当前课程设计演示：不依赖外部向量数据库，条目可在 Git 中审查，且能保留来源、适用边界、证据等级和人工动作。
- 检索返回匹配理由很重要，它能把“为什么命中”暴露给答辩和审计；同分条目排序不应作为稳定业务契约，测试只断言目标条目存在。
- 固定权重的 `fusedScore` 只能作为演示决策指标，不能称为概率校准；视觉和工艺风险不一致时必须保留不一致原因并升级人工复核。
- 无证据场景仍应返回低风险观察和人工判断提示，不应静默生成具体设备调整动作；无知识命中时通过 `fallback=true` 明确降级。
- 所有推荐均设置 `humanApprovalRequired=true`，接口不连接 PLC/MES，也不自动执行批次隔离、参数调整或放行。
- 阶段 6.2 已通过 28/28 Node 测试；未完成浏览器截图验收仍受本地 Codex 浏览器沙箱故障影响，不能伪造视觉结果。
