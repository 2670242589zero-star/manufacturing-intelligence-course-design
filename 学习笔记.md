# 制造智能技术课程设计学习笔记

> 课程设计方向：面向制造质量异常的多源证据融合与知识增强处置推荐平台
>
> 文档状态：选题调研与方案规划阶段

## 1. 课程设计任务理解

本课程设计要求使用人工智能技术解决制造或工业场景中的具体问题，并通过 vibe coding 方法完成一个可运行的 B/S 或 C/S 系统 Demo。项目不能只停留在算法实验或页面展示，需要形成完整业务闭环：前端交互、后端服务、数据库、算法模块、数据处理、测试和演示。

任务书的主要要求包括：

- 面向制造/工业场景，例如生产、质量控制、设备运维、物流仓储或生产管理；
- 使用《制造智能技术》课程中至少 3 个相关技术方向；
- 完成前端 UI、后端 API/服务层、数据库和算法模块；
- 完成数据集收集、清洗、处理与入库；
- 使用 git 管理全过程，保留有意义的提交记录、prompt/会话日志和打卡记录；
- 提交代码仓库、需求规格说明书、设计报告、3 分钟演示视频和过程档案；
- 报告需披露 AI 工具、关键 prompt、AI 出错案例和修正过程；
- 答辩中能够解释 AI 生成代码的作用、输入输出和核心逻辑。

课程总周期为 10 天：D1-D2 学习和调研，D3-D5 选题与方案设计，D6 数据资源整理，D7-D8 详细开发，D9 集成调试和报告，D10 答辩。

## 2. AI 工具学习

### 2.1 Vibe coding 方法

本项目将 vibe coding 理解为一种“人负责目标、约束、验收和判断，AI 负责辅助实现”的开发方法。核心不是让 AI 一次生成完整项目，而是把复杂任务拆成可验证的小步骤，并持续检查结果。

主要方法包括：

1. **提示词工程**：清楚描述任务目标、输入输出、约束条件、技术栈和验收标准。
2. **上下文工程**：让 AI 先阅读项目规则、需求文档、数据字典和现有代码，再提出修改方案。
3. **规格驱动开发**：先写需求规格说明书、接口定义和数据结构，再生成代码。
4. **分步实现**：按照数据库、后端接口、算法模块、前端页面、联调测试的顺序逐步开发。
5. **自动化测试与人工审查**：AI 可以生成测试用例，但测试结果和关键业务逻辑必须人工确认。
6. **人工接管**：当 AI 对需求理解错误、产生重复代码或测试失败时，停止继续生成，重新说明问题并手动修正。
7. **最小复现与小步提交**：每次只解决一个清晰问题，确认可运行后再提交 git。

### 2.2 计划使用的 Harness

Harness 指承载 AI 编程、上下文管理、工具调用和代码修改的开发代理或开发环境。本项目拟采用：

| Harness | 计划用途 | 选择理由 |
|---|---|---|
| Codex | 主力开发和项目协作 | 能读取项目文件、执行命令、修改代码、运行测试并持续跟踪任务上下文 |
| GitHub | 代码托管和过程记录 | 保存代码、Markdown 文档、提交历史和后续协作记录 |

本项目采用 Codex 作为 AI 编程 Harness，使用 GitHub 作为代码托管和过程记录平台。所有 AI 生成内容都要经过运行、测试和人工审查后才能进入主分支。

### 2.3 计划使用的模型

本项目选用 GPT-5.6sol 作为主要编程模型，用于需求拆解、Python 数据处理、FastAPI 接口、算法模块、测试代码和报告辅助。

当前计划如下：

- **选用模型**：GPT-5.6sol，负责需求拆解、Python 数据处理、FastAPI 接口、算法模块、测试代码和报告辅助；
- **最终原则**：模型版本以实际可用环境为准，不把模型输出直接作为最终结论，所有关键代码需要通过测试并由本人理解。

模型选择指标包括：制造场景理解能力、代码正确率、中文交互质量、上下文长度、工具调用能力、响应稳定性和使用成本。

### 2.4 关键 Prompt 记录方式

后续每次重要 AI 交互需要记录：日期、工具、模型、任务目标、输入上下文、关键 prompt、生成结果、测试结果、人工修改和最终是否采纳。

计划使用的 Prompt 模板：

```text
项目背景：这是一个面向制造质量分析的 B/S 系统。
当前任务：实现一个可测试的质量预测 API。
已有上下文：请先阅读需求规格说明书、数据字典和现有目录结构。
约束条件：使用 Python + FastAPI + scikit-learn；不得修改无关文件；保持接口输入输出稳定。
验收标准：给出实现步骤，生成代码后提供测试方法，并说明可能的异常情况。
人工检查点：解释关键函数、模型输入、模型输出以及失败时的处理逻辑。
```

## 3. Git 原理与功能学习

### 3.1 Git 的基本原理

Git 是分布式版本控制系统。项目中的文件变化会形成对象并被提交记录引用，每次 commit 保存一个相对完整的项目状态。不同提交之间通过父子关系形成历史，分支本质上是指向某个提交的可移动引用。

Git 的工作区可以理解为三个区域：

1. **工作区**：当前正在编辑的文件；
2. **暂存区**：通过 `git add` 选择、准备提交的变更；
3. **版本库**：通过 `git commit` 保存的本地历史。

远程仓库是本地版本库的协作副本。通过 `git push` 将本地提交发送到远程仓库，通过 `git fetch` 或 `git pull` 获取远程更新。

### 3.2 常用 Git 功能

```bash
git init                         # 初始化本地仓库
git status                       # 查看工作区和暂存区状态
git add 学习笔记.md              # 将文件加入暂存区
git commit -m "docs: add learning notes"  # 创建提交
git log --oneline                # 查看提交历史
git branch                       # 查看分支
git switch -c feature/data       # 创建并切换分支
git diff                         # 查看未提交的差异
git remote -v                    # 查看远程仓库
git push -u origin main         # 推送主分支
git pull --rebase                # 获取并整理远程更新
```

### 3.3 本项目的 Git 管理约定

- 主分支保持可运行状态；
- 每个提交只解决一个相对独立的问题；
- 提交说明使用统一格式，例如 `docs:`、`feat:`、`fix:`、`test:`；
- 不提交数据集中的隐私信息、密钥、密码和本地环境配置；
- AI 生成代码先测试、再提交；
- 每日保留一次进度提交和一份过程记录；
- 重大修改前创建独立分支，验证后再合并；
- 报告中说明哪些代码由 AI 辅助生成、哪些部分经过人工修改。

## 4. 选题调研

### 4.1 候选方向

| 方向 | 优点 | 风险 | 判断 |
|---|---|---|---|
| 制造质量异常分析 | 表格数据易处理，业务闭环清晰，便于展示预测、异常检测和解释 | 不能把相关性表述为确定因果 | 首选 |
| 设备预测性维护 | 工业价值明显，预测结果直观 | 对时间序列、故障标签和评估方法要求较高 | 备选 |
| 生产交期与排产辅助 | 有管理决策价值 | 需要较多生产约束和优化建模工作 | 备选 |
| 仓储库存预警 | 数据和页面较容易构建 | 容易退化为普通管理系统 | 备选 |
| 视觉表面缺陷检测 | 演示效果好 | 图片标注、训练和部署工作量较大 | 不作为首选 |

### 4.2 最终选题

**题目：基于多源生产数据的制造质量异常分析与根因辅助诊断系统设计与实现。**

系统服务对象为质量工程师或生产管理人员，主要解决以下问题：

- 生产数据分散，质量人员难以快速了解数据质量；
- 传统规则难以发现未知异常模式；
- 质量预测结果缺少可解释性；
- 异常发生后需要人工对多个生产特征进行对比分析。

系统不宣称自动确定真实根因，而是输出质量风险、异常样本、重要特征和根因候选，供质量人员复核。

### 4.3 技术方向映射

本项目计划覆盖 4 个制造智能技术方向：

1. **数据预处理与特征工程**：缺失值、异常值、编码、标准化和特征统计；
2. **监督学习分类**：预测产品缺陷类别或质量风险等级；
3. **异常检测**：识别偏离正常生产规律的异常样本和批次；
4. **可解释分析与可视化**：展示特征重要性、分组差异和诊断候选。

### 4.4 数据源调研方向

优先调研 UCI Steel Plates Faults 钢板缺陷数据集，也可以将 AI4I 2020 Predictive Maintenance 数据集作为设备方向备选。首个版本只选一个主数据集，避免多套数据无法对齐。

数据调研需要记录：

- 数据来源和引用方式；
- 样本数量、字段含义和标签定义；
- 缺失值、异常值和类别不平衡情况；
- 训练集、验证集和测试集划分方法；
- 数据清洗规则和特征构建过程；
- 数据集许可证和允许使用范围。

### 4.5 调研关键词

- 制造质量异常检测
- 工业过程质量预测
- 钢板表面缺陷分类
- manufacturing quality prediction
- industrial anomaly detection
- explainable AI for manufacturing
- root cause analysis in manufacturing
- predictive quality in smart manufacturing

## 5. 系统规划

### 5.1 视觉质检融合架构

在原有生产质量异常分析系统中加入视觉质检模块是可行的。视觉模块负责从产品图像中提取缺陷信息，质量分析模块负责融合图像特征和生产过程数据，最终输出质量风险与辅助决策。

```text
                 智能视觉质检平台
                          |
       +------------------+------------------+
       |                  |                  |
       v                  v                  v
   图像预处理         缺陷检测模型       质量分析模型
       |                  |                  |
     OpenCV          YOLO/检测模型       分类/预测算法
       |                  |                  |
       +------------------+------------------+
                          v
                       质量决策
                          v
              数据库 + 可视化 Dashboard
```

建议的业务数据流为：图像上传 → OpenCV 预处理 → YOLO 缺陷检测 → 提取缺陷类别、位置、面积和置信度 → 与批次号、产品号、时间和生产参数关联 → 质量风险预测 → Dashboard 展示 → 人工复核。

### 5.2 视觉模块与原系统的接口

视觉检测结果不直接替代原有质量分析模型，而是作为新的质量特征输入。例如：缺陷数量、最大缺陷面积、缺陷置信度、缺陷类别、图像清晰度和检测区域比例，都可以与温度、压力、速度等生产过程特征一起用于质量分析。

建议的核心数据字段如下：

| 字段 | 作用 |
|---|---|
| batch_id | 关联生产批次和质量记录 |
| product_id | 关联具体产品 |
| image_path | 保存原始图像或对象存储地址 |
| defect_type | 缺陷类别 |
| bbox | 缺陷位置 |
| defect_area | 缺陷面积或面积比例 |
| confidence | 模型置信度 |
| process_features | 温度、压力、速度等生产参数 |
| quality_score | 综合质量风险分数 |
| review_status | 人工复核状态 |

### 5.3 十天周期内的范围控制

完整视觉系统的工作量高于表格数据系统，因此采用分层目标：

- **必须完成**：图像上传、OpenCV 预处理、一个公开缺陷数据集、YOLO 或现成检测模型推理、检测框可视化、检测结果入库、Dashboard 展示；
- **建议完成**：将缺陷数量、面积和置信度作为特征，输入质量风险模型；
- **时间充足再做**：YOLO 模型微调、过程参数与图像数据的严格时间对齐、根因候选排序和模型版本管理；
- **不建议首版加入**：同时训练目标检测和图像分类两个模型、实时相机接入、复杂边缘设备部署。

视觉部分最好只选择“目标检测”或“图像分类”中的一个作为主任务。若已有带边界框的数据，选择 YOLO 目标检测；若只有按类别标注的图片，选择图像分类，避免在十天内同时处理两类标注体系。

### 5.4 融合后的技术方向映射

融合架构可覆盖以下技术方向：

1. OpenCV 图像预处理；
2. YOLO 目标检测或工业缺陷分类；
3. 生产过程数据的监督学习质量预测；
4. 异常检测、特征重要性和可视化分析；
5. 数据库、API 和前端 Dashboard 集成。

因此，从课程要求看，该方案不仅可行，而且能够形成较清晰的“感知—分析—决策—复核”制造智能闭环。主要风险是图像数据集、标注质量和模型训练时间，需要优先采用公开数据和预训练模型。

### 5.1 业务流程

```text
数据导入 → 数据清洗 → 特征处理 → 数据入库
       → 质量风险预测 → 异常样本检测
       → 特征重要性分析 → 根因候选排序
       → 人工复核 → 保存分析记录
```

### 5.2 系统模块

- 数据管理：导入、字段查看、数据质量检查；
- 质量预测：输入生产参数，返回缺陷类别和风险概率；
- 异常检测：显示异常样本、异常分数和异常特征；
- 诊断分析：展示特征重要性、趋势和分组对比；
- 模型评估：准确率、混淆矩阵、分类报告；
- 结果记录：保存预测时间、结果、人工复核意见；
- 系统说明：展示数据来源、模型局限和 AI 使用说明。

### 5.3 推荐架构

- 前端：Vue 或 React + ECharts；
- 后端：FastAPI；
- 数据库：SQLite，必要时迁移 PostgreSQL；
- 算法：Pandas、NumPy、scikit-learn；
- 测试：Pytest 和接口测试；
- 代码管理：Git + GitHub。

## 6. 十天实施计划

| 时间 | 任务 | 产出 |
|---|---|---|
| D1-D2 | 学习 AI 编程工具、阅读任务书、完成选题调研 | 调研记录、候选题目 |
| D3-D5 | 确定题目、需求、技术映射和系统架构 | 需求说明书、架构图 |
| D6 | 下载、清洗、分析和入库数据 | 数据字典、清洗脚本 |
| D7-D8 | 开发后端、算法、前端并完成联调 | 可运行 Demo |
| D9 | 自动化测试、修复问题、编写报告和 PPT | 测试记录、报告、PPT |
| D10 | 录制视频、准备代码讲解和答辩 | 最终提交材料 |

## 7. AI 使用和质量控制

AI 生成代码不能直接作为最终成果。每次生成后需要：

1. 阅读代码并理解变量、函数和数据流；
2. 检查是否符合需求和项目架构；
3. 运行自动化测试和手工场景测试；
4. 对错误进行定位、修改和再次测试；
5. 在过程档案中记录 AI 的错误和修正方法；
6. 使用 git 保存可追溯的修改历史。

重点风险包括数据泄漏、训练集与测试集混用、指标解释错误、把相关关系误写为因果关系，以及 AI 生成接口与前端调用不一致。

## 8. 后续待办

- [ ] 完成 GitHub 远程仓库创建和首次推送；
- [ ] 确认主数据集并保存数据说明；
- [ ] 完成需求规格说明书；
- [ ] 绘制系统架构图和数据库设计图；
- [ ] 建立前后端项目目录；
- [ ] 记录每次 AI 工具和模型使用过程；
- [ ] 完成自动化测试和 3 分钟演示视频。

## 9. 参考资料

1. 《制造智能技术课程设计任务书》；
2. 张智海等：《制造智能技术基础》，清华大学出版社，2022；
3. Git 官方文档：https://git-scm.com/doc；
4. GitHub Docs：https://docs.github.com/；
5. UCI Machine Learning Repository：https://archive.ics.uci.edu/；
6. GitHub、CSDN、B 站等 vibe coding 学习资料。

## 阶段实现记录：模型适配与数据接入

### OpenCV / YOLO 适配策略

本阶段增加 `algorithm/inference-adapter.js`，将质量分析调用分为两种模式：

- 本地模式：使用 Canvas 图像指标和可解释规则评分，便于离线演示与自动化测试。
- 远程模式：通过 `VISION_MODEL_ENDPOINT` 调用外部 `/infer`，为 OpenCV 预处理和 YOLO 缺陷检测服务预留统一接口。

远程服务失败时允许显式降级到本地算法。系统通过 `/api/model-status` 披露当前适配器、最后一次请求和降级状态，避免把规则算法误称为 YOLO 推理。

### 公开数据调研与安全边界

在 `data/dataset-sources.json` 登记 NEU 表面缺陷、Severstal 钢材缺陷和 DAGM 2007 三类候选数据源。仓库只提交来源元数据与导入工具，不直接再分发原始图片；使用前需核对来源条款。`scripts/import-dataset.js` 生成本地图片清单，原始数据和清单目录由 Git 忽略。

### AI 出错案例与人工修正

最初将测试命令改为 `node --test test`，希望只扫描正式测试目录。但当前 Node.js 将 `test` 解释为待执行模块，返回 `MODULE_NOT_FOUND`。人工检查错误后改为 `node --test "test/*.test.js"`，最终正式测试集 11 项全部通过。这说明 AI 给出的命令仍需在目标运行环境中验证，不能只根据语义判断其可用性。

## YOLO11n 真实训练记录

### 环境选择

最初准备建立独立 Python 3.12 环境，但 CUDA PyTorch 安装包约 2.6GB。根据人工决策改为检查并复用现有 Conda 环境 `cv_tutorial`。该环境已具备 Python 3.10.20、PyTorch 2.5.1 CUDA 12.1、Ultralytics 8.4.91，`torch.cuda.is_available()` 为 true，可直接使用 RTX 4060 Laptop GPU。

### 数据准备

采用 NEU-DET 钢材表面缺陷检测数据，共 1800 张图片和 6 类缺陷框标注。整理为训练 1440 张、验证 180 张、独立测试 180 张。Ultralytics 扫描未发现损坏图片，但发现 3 个完全重复标注框并自动去重。

### 训练参数

- 模型：YOLO11n 官方预训练权重迁移学习。
- 输入尺寸：320×320。原图为 200×200，不采用无意义的 640 放大。
- 训练轮数：100；batch 32；AdamW；AMP；seed 42。
- GPU：RTX 4060 Laptop 8GB，训练显存约 1.4GB。
- 总训练时间：约 0.264 小时。

### 训练结果

验证集最佳结果：Precision 0.685、Recall 0.764、mAP50 0.765、mAP50-95 0.451。

独立测试集结果：Precision 0.676、Recall 0.692、mAP50 0.747、mAP50-95 0.402。测试指标与验证指标接近，暂未观察到明显过拟合。crazing 类别较弱，应作为后续数据与增强策略优化重点。

### 模型编译与部署验证

最佳 PyTorch 权重已导出为固定输入 320×320 的 ONNX 模型，ONNX checker 校验通过。使用 ONNX Runtime CPU 对 patches 测试图像推理，输出 3 个 patches 检测框，最高置信度 0.833，证明导出模型可以实际加载和推理。

本阶段数据、权重、ONNX 和训练结果仅保存在本地，没有提交或上传 GitHub。

