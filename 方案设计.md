# 方案设计

## 一、项目概述

项目名称：**面向制造质量异常的多源证据融合与知识增强处置推荐平台**。

系统面向制造质量控制场景，接收产品图像分析结果、工艺参数、批次信息、历史检测记录和质量知识条目，经过数据清洗、质量风险预测、异常模式识别和知识检索，输出异常摘要、证据排序、候选处置建议和人工复核记录。

系统定位为质量工程师的辅助分析工具，重点解决信息分散、异常解释困难和经验难以复用的问题。

## 二、需求分析

### 2.1 用户角色

| 用户 | 主要需求 |
|---|---|
| 质量工程师 | 查看异常批次、证据来源、模型结果和处置建议 |
| 工艺工程师 | 对照工艺参数趋势、规则阈值和历史案例 |
| 课程设计评审者 | 查看系统架构、算法过程、测试结果和 AI 使用记录 |

### 2.2 功能需求

1. **数据接入**：录入图像指标、检测框/缺陷类别、工艺参数、批次号和时间。
2. **质量风险分析**：运行质量回归模型或风险评分模型，输出预测值、风险等级和置信信息。
3. **异常模式识别**：检测参数偏离、历史少见组合和批次异常，形成异常证据列表。
4. **证据融合**：按照来源、时间、模型置信度和规则匹配结果合并证据，避免重复计分。
5. **知识检索**：根据异常特征检索质量规则、历史案例和候选处置措施。
6. **推荐确认**：展示建议、推荐依据和适用条件，支持人工确认、驳回和填写备注。
7. **可视化 Dashboard**：展示风险分布、参数趋势、异常批次、证据贡献和处理状态。
8. **数据追溯**：保留输入、模型版本、知识条目、推荐结果、人工动作和时间戳。

### 2.3 非功能需求

- 可解释：每条建议必须关联证据和知识条目，不输出无法追溯的结论。
- 可测试：算法函数、API 和核心页面流程均提供可重复测试入口。
- 可降级：模型服务不可用时，使用本地规则和已保存模型完成演示，并明确显示降级状态。
- 可复现：记录数据版本、训练参数、环境版本和随机种子。
- 安全：不在前端暴露密钥，不上传原始大数据和敏感信息。

## 三、总体架构

```text
+----------------------------------------------------------+
|                     Web Dashboard                        |
| 数据导入 | 风险总览 | 异常详情 | 推荐确认 | 追溯记录       |
+--------------------------+-------------------------------+
                           | REST API
+--------------------------v-------------------------------+
|                    Node.js 服务层                         |
| 参数校验 | 流程编排 | 权限边界 | 状态记录 | 降级处理       |
+---------------+----------------------+--------------------+
                |                      |
+---------------v--------------+ +-------v----------------+
| 多源证据分析模块              | | 知识增强推荐模块       |
| 图像/YOLO11n 结果             | | 规则库、历史案例       |
| 工艺参数质量模型              | | 关键词检索/RAG 接口    |
| 异常检测与特征贡献            | | 建议生成与人工确认     |
+---------------+--------------+ +-------+----------------+
                |                      |
                +----------+-----------+
                           v
                  JSON/SQLite 数据存储
           检测记录、批次、模型、知识、审计日志
```

## 四、技术方案论证

### 4.1 为什么选择多源证据融合

单一视觉模型只能回答“图像中检测到了什么”，单一工艺模型只能回答“参数对应的质量指标可能是多少”。将两类结果与历史批次及工艺规则组合后，系统可以形成更完整的异常上下文，适合课程要求中的前后端、算法和数据管理综合展示。

### 4.2 为什么采用 Random Forest 作为工艺参数基线模型

工艺数据通常包含非线性关系、不同量纲和一定噪声。随机森林对特征缩放不敏感，训练和解释成本较低，适合课设周期内建立稳定基线。模型输出用于质量风险预测和证据排序，不宣称对工艺变量有确定因果解释。后续可与梯度提升、SVR 或时序模型进行对比。

### 4.3 为什么采用规则库加轻量检索作为知识增强起点

课程设计需要可解释、可演示和可复现。先使用 JSON/SQLite 保存“异常模式—检查项目—候选措施—适用条件—来源”条目，通过关键词和字段匹配完成检索；在核心链路稳定后，再接入向量检索或大语言模型。这样可以在外部模型不可用时保持系统可运行。

## 五、数据集与数据处理

### 5.1 数据来源

1. **NEU-DET**：用于验证 YOLO11n 的钢材表面缺陷检测链路，原始图片和权重仅保存在本地。
2. **Quality Prediction in a Mining Process**：用于工艺参数质量回归实验，目标为 `% Silica Concentrate`，包含矿浆、药剂流量、pH、密度、浮选柱气流和液位等过程变量。
3. **本地模拟批次数据**：用于演示视觉结果、工艺参数结果、知识条目和人工复核状态之间的关联，不冒充真实企业数据。

### 5.2 处理流程

```text
原始 CSV/检测结果
      |
编码与字段检查、缺失值统计、重复值处理
      |
按时间排序并进行小时聚合
      |
异常值检查、特征构造、目标列与输入列隔离
      |
按时间顺序切分 train/validation/test
      |
训练模型、保存元数据、评估和导出
```

处理原则：不随机打乱具有时间顺序的数据；排除与目标同时产生且可能造成泄漏的产品结果字段；将数据清洗规则、特征清单和数据集版本写入模型卡。

## 六、算法设计

### 6.1 工艺参数质量模型

- 输入：过程参数统计特征、小时聚合特征和时间上下文特征；
- 模型：RandomForestRegressor 基线；
- 输出：质量指标预测值、预测误差、风险分数和特征重要性；
- 评估：MAE、RMSE、R²，并与训练集均值基线比较；
- 导出：joblib 保存 Python 模型，条件允许时导出 ONNX 供部署验证。

### 6.2 异常检测

使用标准化后的过程特征计算工况偏离程度，并使用 Isolation Forest 识别训练数据中少见的组合。异常检测结果只作为证据之一，结合时间窗口、模型残差和业务阈值生成综合风险。

### 6.3 视觉证据

YOLO11n 输出缺陷类别、置信度和检测框；平台将英文类别映射为中文质量术语，并汇总检测数量、最大置信度和缺陷面积比例。视觉结果不直接等同于最终质量结论。

### 6.4 证据融合与推荐

建议的融合过程：

1. 对视觉、工艺模型、异常检测和业务规则结果统一格式；
2. 按批次号和时间窗口关联多源记录；
3. 对各证据进行置信度校准和来源标记；
4. 根据风险等级选择知识检索关键词；
5. 返回“异常摘要—证据—推荐—适用条件—人工确认”的结构化结果。

推荐结果中保留 `modelVersion`、`evidenceIds`、`knowledgeIds` 和 `reviewStatus`，便于复核和审计。

## 七、前端与后端规划

### 7.1 前端 UI

- 顶部：平台名称、模型服务状态、数据时间范围；
- 总览区：批次总数、异常批次、平均风险、待复核建议；
- 趋势区：质量预测、工艺参数、异常分数随时间变化；
- 详情区：异常摘要、证据贡献、视觉结果、参数偏离和知识建议；
- 操作区：确认建议、驳回建议、填写复核备注和导出记录。

### 7.2 后端 API

| API | 作用 |
|---|---|
| `GET /api/health` | 服务健康检查 |
| `GET /api/pipeline` | 查看流水线和模型适配状态 |
| `GET /api/summary` | 获取总览统计 |
| `GET /api/inspections` | 查询检测和异常记录 |
| `POST /api/inspect` | 提交图像指标与工艺参数 |
| `GET /api/model-status` | 查看模型/远程推理状态 |
| `POST /api/recommendations/review` | 保存人工确认结果，后续实现 |

## 八、外部资源与环境配置

- Harness：Codex，用于需求拆解、代码实现、测试和过程记录；
- 代码托管：GitHub，公开仓库只保存代码、Markdown 文档、元数据和小型样例；
- 主要编程模型记录：GPT-5.6sol；
- JavaScript：Node.js，运行 B/S Demo 和测试；
- Python：`cv_tutorial` Conda 环境，运行 pandas、scikit-learn、joblib、Ultralytics 和 ONNX Runtime；
- GPU：RTX 4060 Laptop GPU，用于 YOLO11n 训练；
- 可选部署：OpenCV 预处理服务、ONNX Runtime 推理服务；
- 不依赖项：外部大模型不可用时，使用本地规则库和结构化检索完成推荐演示。

## 九、计划安排

| 阶段 | 主要工作 | 产出 |
|---|---|---|
| 第 1 阶段 | 任务书分析、班级选题去重、需求确认 | 选题说明、需求清单 |
| 第 2 阶段 | 数据下载、清洗、字段分析和数据字典 | 数据说明、处理脚本 |
| 第 3 阶段 | 训练工艺参数模型和异常检测基线 | 模型文件、评估报告 |
| 第 4 阶段 | 接入 YOLO11n，统一多源证据格式 | 推理适配层、联调记录 |
| 第 5 阶段 | 建立知识库、推荐接口和人工复核流程 | 知识条目、推荐 API |
| 第 6 阶段 | 完成 Dashboard、自动化测试和异常场景测试 | 可运行 Demo、测试记录 |
| 第 7 阶段 | 完成报告、视频、答辩问答和过程材料 | 设计报告、演示视频 |

## 十、测试与验收标准

- 单元测试覆盖数据清洗、风险评分、证据融合和知识检索；
- API 测试覆盖正常输入、缺字段、模型降级和无匹配知识条目；
- 模型测试至少报告验证集和独立测试集指标；
- ONNX 与 Python 模型对同一输入的预测结果在容许误差内一致；
- 手工演示能够完成“导入批次—发现异常—查看证据—获得建议—人工确认—追溯记录”；
- 页面不显示无法解释来源的自动因果结论。

## 十一、参考文献与资料

1. 《制造智能技术课程设计任务书》。
2. Breiman, L. Random Forests. Machine Learning, 2001.
3. Liu, F. T., Ting, K. M., Zhou, Z.-H. Isolation Forest. ICDM, 2008.
4. Ultralytics YOLO 文档与 YOLO11 模型说明。
5. scikit-learn User Guide：Ensemble methods、Outlier detection。
6. NEU Surface Defect Database 数据说明。
7. Quality Prediction in a Mining Process 公开数据集说明。
8. Git 官方文档与 GitHub Docs。

## 十二、实施边界

本方案优先保障可运行、可测试和可解释。大语言模型只用于开发辅助或在知识检索结果上生成自然语言摘要；模型输出、知识条目和人工确认状态均需保留。任何生产部署前还需要补充现场数据校准、权限控制、模型监控、漂移检测和工艺安全评审。
