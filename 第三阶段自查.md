# 第三阶段自查

自查日期：2026-08-28

## 1. 数据来源

- [x] `data/` 已包含可直接审查的小型数据文件 `process-quality-sample.csv`，共 10 行、13 列。
- [x] 原始公开数据来源为 [Quality Prediction in a Mining Process](https://www.kaggle.com/datasets/edumagalhaes/quality-prediction-in-a-mining-process)。
- [x] 许可登记为 `CC0: Public Domain`，已同步写入根 `README.md`、`data/README.md`、数据卡和 `dataset-sources.json`。
- [x] 加工样例公开地址为 GitHub 仓库中的 `data/process-quality-sample.csv`。
- [x] 已准确注明为“基于 CC0 原始公开数据加工的 10 行教学样例”，不表述为企业自建生产数据。
- [x] 完整原始下载、NEU-DET 图片、训练权重和运行输出未纳入公开提交。

## 2. 数据预处理

- [x] 预处理程序为 `scripts/preprocess-process-quality.js`。
- [x] 复现命令为 `npm run dataset:preprocess`。
- [x] 程序校验原始 24 列表头，解析分号分隔和逗号小数，转换 UTC ISO 8601 时间，筛选代表性字段并聚合 7 个浮选柱气流/液位字段。
- [x] 抽取的原始数据行号为 `1-4,188-193`，数据行从 1 开始且不含表头。
- [x] 预处理结果为 `data/process-quality-sample.csv`。
- [x] 审计索引为 `data/process-quality-index.json`，包含原始 SHA-256、抽取行号、转换规则、输出 SHA-256、记录数和字段数。
- [x] 单元测试覆盖行号选择、数字格式、时间格式、聚合均值、输出结构和索引哈希一致性。

## 3. AI 工具提示词追溯

- [x] 已创建 `prompt/` 目录及目录说明。
- [x] 已保存 `prompt/2026-08-26-stage-03-data-and-prompt.json`。
- [x] 已保存 `prompt/2026-08-28-stage-03-public-dataset.json`。
- [x] 记录包含 Harness `Codex`、模型 `GPT-5.6sol`、用户目标、执行动作、数据来源、安全边界、备份和上传状态。
- [x] 提示词记录不包含 Token、密码、Cookie、企业敏感数据或完整原始数据。
- [x] 后续每个阶段继续追加或更新 JSON，并在上下文压缩前优先备份 `prompt/`、`progress.md` 和 `task_plan.md`。

## 4. 发布前安全检查

- [x] `.gitignore` 排除 `data/local/`、`models/`、`runs/`、`training/runs/` 和 `_backups/`。
- [x] Git 提交前检查暂存文件，不上传原始数据、权重、Token、Cookie、运行目录或本地备份。
- [x] 阶段完成后创建本地最终备份，再提交并推送 `origin/main`。
- [x] 推送后核对本地 `HEAD` 与 `origin/main` 一致。
