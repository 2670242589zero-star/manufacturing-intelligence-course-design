# 第六阶段自查

自查日期：2026-08-30

## 1. YOLO11n 推理服务

- [x] 已创建 `inference/yolo_service.py`，使用 OpenCV 解码图片并调用 Ultralytics YOLO11n。
- [x] 已提供 `GET /health` 和 `POST /infer` 接口。
- [x] 已复用本地训练权重 `best.pt`，未将权重上传到 GitHub。
- [x] 已将 Ultralytics 配置目录设置为项目内 `.ultralytics/`，并通过 `.gitignore` 排除。
- [x] 返回内容包含缺陷类别、中文标签、置信度、面积、检测框、质量分、风险等级、贡献因素和分析时间。

## 2. 平台真实图片链路

- [x] 前端可读取 PNG、JPEG 或 WEBP 图片为 Base64 data URL。
- [x] 浏览器端限制图片不超过 6 MB，Node 请求体限制为 8 MB，图片字段限制为 7 MB。
- [x] `POST /api/inspect` 会校验图片格式并将原图转发给推理适配器。
- [x] 前端可依据推理结果绘制检测框。
- [x] 远程模型服务不可用时仍保留可解释规则降级模式，并在方法字段中明确标记 fallback。

## 3. 测试与真实联调

- [x] Node、前端 JavaScript 和 Python 服务语法检查通过。
- [x] 自动化测试通过 19/19。
- [x] 测试覆盖图片数据转发、非法图片格式拒绝、远程推理和本地降级。
- [x] 已使用 NEU-DET 测试图 `crazing_271.jpg` 完成 Node 到 Python 的端到端联调。
- [x] 联调返回 HTTP 201、质量分 39、中风险、2 个裂纹候选，方法为 `OpenCV preprocessing + YOLO11n local service`。
- [x] 测试记录执行后已恢复，测试图片和推理结果未加入公开仓库。

## 4. 数据、安全与过程追溯

- [x] 原始 NEU-DET 图片、训练权重、训练输出、Ultralytics 配置和本地备份均被 Git 忽略。
- [x] 本阶段未新增自建数据集，也未将公开数据误称为自建数据。
- [x] 已更新 `prompt/2026-08-29-stage-06-yolo-inference.json`、`progress.md` 和 `task_plan.md`。
- [x] 提交前执行 `git diff --check`，并检查待提交范围不包含凭据、模型权重或原始数据。
- [x] 阶段完成后创建 `_backups/step-09-yolo-inference/final` 本地备份。
