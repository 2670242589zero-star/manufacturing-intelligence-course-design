# 制造智能技术课程设计报告

## 面向制造质量异常的多源证据融合与知识增强处置推荐平台

| 项目 | 内容 |
|---|---|
| 课程名称 | 制造智能技术课程设计 |
| 姓名 | [张孝通] |
| 班级 | [智造24.1] |
| 学号 | [202412020127] |
| 完成日期 | 2026 年 9 月 3 日 |

## 摘要

制造质量异常分析通常同时涉及产品表面图像、工艺参数、历史检测结果和质量经验。单一视觉模型只能识别图像中的缺陷候选，单一工艺模型只能估计参数对应的质量指标，难以直接形成可追溯的处置依据。为此，本课程设计实现了一个面向制造质量异常的多源证据融合与知识增强处置推荐平台。

系统采用 B/S 架构。浏览器 Dashboard 提供视觉检测、工艺预测、证据查看、知识建议、人工复核和审核追溯功能；Node.js 服务负责 REST API、参数校验、模型编排、状态记录和降级处理；Python 服务分别运行 YOLO11n 缺陷检测模型和 RandomForestRegressor 工艺质量模型；JSON 知识库存储异常模式、检查项目、候选处置措施、适用边界和停止条件；SQLite 保存不可变推荐决策快照和追加式人工复核。平台将视觉、工艺和历史结果统一为结构化证据，通过受控融合档案形成演示型排序分数，再检索知识条目并输出要求人工审批的候选建议。

视觉模型使用 NEU-DET 数据集完成 6 类钢材表面缺陷迁移学习，独立测试集 Precision 为 0.676、Recall 为 0.692、mAP50 为 0.747、mAP50-95 为 0.402。工艺模型使用公开矿物加工质量数据，737453 条原始记录按小时聚合为 4097 条，采用时间顺序切分；测试 MAE 为 0.87194、RMSE 为 1.11653、R² 为 0.13100，相对中位数基线的 MAE 改善为 10.21%。两个模型均完成部署格式验证，平台自动化测试 33 项全部通过。

结果表明，本系统完成了数据预处理、深度学习、监督学习、知识表示、多源融合、前后端集成和可视化的教学链路。由于工艺模型解释能力有限、知识库规模较小、融合权重未经概率校准，系统仅用于课程演示和辅助分析，不用于自动根因判定或生产设备控制。

**关键词：** 制造质量；YOLO11n；随机森林；多源证据融合；知识库；处置推荐

## 1. 绪论

### 1.1 研究背景

制造现场的质量异常往往不是由单一信息源决定。产品表面可能出现裂纹、划痕、夹杂等缺陷，同时工艺温度、流量、压力、速度或设备状态也可能发生偏离。质量工程师通常需要在多个系统、报表和经验规则之间来回核对，存在信息分散、分析时间长、建议依据难追溯等问题。

机器视觉可以提高表面缺陷发现效率，机器学习可以从历史工艺数据中建立质量预测基线，知识库可以保存检查流程和处置边界。将这些技术组合起来，可以构建“发现异常—汇总证据—检索知识—人工决策”的辅助分析流程。

### 1.2 研究意义

本项目的意义主要体现在三个方面：

1. 将视觉检测和工艺质量预测放在同一业务流程中，避免把单一模型结果直接当作最终结论；
2. 通过证据来源、匹配理由和知识条目提高推荐结果的可解释性；
3. 形成包含数据、算法、接口、前端、测试、AI 使用记录和 Git 过程管理的完整课程设计成果。

### 1.3 研究内容

本课程设计完成以下工作：

- 整理 NEU-DET 视觉数据并训练 YOLO11n；
- 清洗公开工艺质量数据并训练 RandomForestRegressor；
- 导出并验证 ONNX 部署模型；
- 设计视觉、工艺和历史证据统一结构；
- 建立轻量规则知识库和处置推荐模块；
- 开发 Dashboard、Node REST API 和 Python 推理服务；
- 完成端到端联调、自动化测试和过程追溯。

## 2. 需求分析

### 2.1 用户角色

| 用户角色 | 主要需求 |
|---|---|
| 质量工程师 | 查看缺陷候选、风险、证据来源和候选处置建议 |
| 工艺工程师 | 核对工艺参数、范围告警、质量预测和相关知识 |
| 课程评审者 | 检查架构、模型训练、测试证据、AI 使用和实现边界 |

当前系统只在业务分析层定义角色，没有实现登录和权限隔离。

### 2.2 功能需求

1. 提交产品图片和批次信息，获得缺陷类别、置信度和检测框；
2. 输入 9 个聚合工艺特征，获得质量预测、风险和范围告警；
3. 将视觉、工艺和历史结果转换为统一证据；
4. 根据证据计算演示型融合风险并保留冲突原因；
5. 检索质量知识条目，输出检查步骤、候选措施和停止条件；
6. 在 Dashboard 展示模型状态、检测结果、预测结果、证据和记录；
7. 保存推荐决策快照，由授权人员确认、驳回或暂缓并追溯历史；
8. 在模型服务不可用时提供明确的错误或降级状态。

## 3. 总体方案与系统架构

### 3.1 技术路线

```text
公开数据与本地图片
        |
        v
数据检查、清洗、格式转换、时间聚合
        |
        +--------------------+
        |                    |
        v                    v
YOLO11n 缺陷检测      RandomForest 质量预测
        |                    |
        +----------+---------+
                   v
             统一证据结构
                   |
                   v
        受控融合档案与冲突检查
                   |
                   v
             JSON 知识库检索
                   |
                   v
        候选检查/处置建议 + 人工审批
                   |
                   v
       SQLite 决策/复核审计 + Dashboard
```

### 3.2 系统架构

系统包含以下层次：

1. **表现层：** 浏览器 Dashboard，负责图片选择、工艺参数输入、结果展示和记录查询；
2. **服务编排层：** Node.js 内置 HTTP 服务，默认端口 4173；
3. **模型服务层：** YOLO11n 服务默认端口 8000，RandomForest 服务默认端口 8010；
4. **算法决策层：** 视觉适配、工艺适配、证据融合、知识检索和推荐模块；
5. **数据层：** JSON 检测记录、知识条目、SQLite 决策/复核审计、数据元信息和本地模型文件。

### 3.3 技术栈

| 层次 | 技术 |
|---|---|
| 前端 | HTML、CSS、原生 JavaScript、Canvas |
| 平台后端 | Node.js 内置 HTTP API |
| 视觉模型 | Python、PyTorch、Ultralytics YOLO11n、OpenCV、ONNX Runtime |
| 工艺模型 | pandas、scikit-learn、joblib、ONNX Runtime |
| 知识与融合 | JavaScript、JSON 规则库、受控同义词与筛选 |
| 审核存储 | Node.js `node:sqlite`、SQLite、CSV |
| 测试 | Node.js Test Runner、HTTP 联调、Python 模型验证 |
| 过程管理 | Codex、GPT-5.6sol、Git、GitHub、Markdown、Prompt JSON |

### 3.4 设计原则

系统设计遵循以下原则：

1. **多源证据相互补充：** 视觉检测、工艺预测和历史信息分别保留来源，不把任一模型输出直接视为最终质量结论；
2. **人机协同决策：** 算法负责整理证据和生成候选措施，质量处置由人工确认，系统不自动控制 PLC/MES；
3. **可解释与可追溯：** 推荐结果关联融合档案、知识条目、匹配理由、模型版本和审核记录；
4. **可降级运行：** 外部模型服务不可用时明确显示状态，并保留本地规则链路，避免产生静默错误；
5. **可复现验证：** 固定数据划分、随机种子和训练参数，通过模型报告、接口测试、Prompt 记录和 Git 差异保存过程证据。

## 4. 数据资源与预处理

### 4.1 NEU-DET 数据集

NEU-DET 是钢材表面缺陷检测数据集，共 1800 张图片和 6 类缺陷。项目将数据转换为 Ultralytics YOLO 格式，并按 1440/180/180 划分训练、验证和独立测试集。

预处理流程包括：

1. 检查图片与标注是否一一对应；
2. 扫描损坏图片和空标注；
3. 将标注转换为归一化 YOLO 框格式；
4. 按固定随机种子划分数据集；
5. 生成数据配置文件和索引；
6. 在训练前由 Ultralytics 再次扫描标签。

扫描未发现损坏图片，3 个完全重复标注框由工具自动去重。完整图片因体积和再分发边界保留本地，项目文档保存来源与复现说明。

### 4.2 工艺质量数据集

工艺模型使用 Kaggle `Quality Prediction in a Mining Process` 公开数据集。数据包含时间、矿浆性质、药剂流量、浮选柱气流和液位以及精矿质量指标。

原始数据为 737453 条、24 列。预处理步骤如下：

1. 规范列名和小数格式；
2. 解析时间并按时间排序；
3. 处理重复时间和缺失值；
4. 将高频传感器数据按小时聚合；
5. 将 21 个浮选柱原始字段汇总为可部署特征；
6. 排除后验质量字段 `iron_concentrate_pct`；
7. 仅根据训练集统计输入范围；
8. 按时间顺序切分训练、验证和测试集。

小时聚合后得到 4097 条记录，训练集、验证集和测试集分别为 2867、615 和 615 条。时间切分比随机切分更接近对未来工况进行预测的部署场景。

### 4.3 数据泄漏控制

数据泄漏会使测试指标虚高。本项目采取了三项控制：

- 排除与预测目标同时或事后获得的质量字段；
- 使用时间顺序切分，不随机混合未来记录；
- 输入范围、缺失值策略和模型参数只由训练数据确定。

## 5. 模型与算法设计

### 5.1 YOLO11n 缺陷检测模型

YOLO11n 作为轻量目标检测模型，适合在课设周期内完成 GPU 迁移学习、部署导出和本地推理。训练使用官方预训练权重，参数如下：

| 参数 | 设置 |
|---|---|
| 输入尺寸 | 320×320 |
| 训练轮数 | 100 |
| Batch size | 32 |
| 优化器 | AdamW |
| 混合精度 | AMP |
| 随机种子 | 42 |
| 设备 | RTX 4060 Laptop GPU |

模型输出缺陷类别、置信度和边界框。平台对英文类别进行中文映射，并汇总检测数量、最大置信度和视觉风险。视觉结果只是质量证据之一，不直接等于最终质量判定。

### 5.2 RandomForest 工艺质量模型

工艺模型使用 `RandomForestRegressor`。随机森林能够表达非线性关系，对特征缩放不敏感，且能提供全局特征重要性，适合作为公开数据上的教学基线。

模型输入为 9 个小时聚合特征，输出精矿二氧化硅质量指标预测值。平台根据预测值形成教学风险等级，并比较输入与训练集范围，生成范围告警。特征重要性只能解释模型关联强度，不能直接说明工艺因果关系。

### 5.3 证据融合

系统将每项证据统一为来源、描述、强度、权重、风险和元数据。阶段 7.2 提供三种受控融合档案：

| 档案 | 视觉检测 | 视觉风险 | 工艺风险 | 范围告警 | 历史风险 | 中/高风险阈值 |
|---|---:|---:|---:|---:|---:|---|
| `balanced` | 0.45 | 0.30 | 0.35 | 0.30 | 0.20 | 0.40 / 0.67 |
| `conservative` | 0.40 | 0.30 | 0.35 | 0.45 | 0.30 | 0.35 / 0.60 |
| `vision-first` | 0.55 | 0.35 | 0.25 | 0.20 | 0.15 | 0.40 / 0.67 |

客户端只能选择档案，不能直接提交任意权重。由于权重和阈值来自教学设计而非统计校准，`fusedScore` 只能解释为决策排序指标，不能解释为异常发生概率；接口显式返回 `calibratedProbability=false`。

当视觉与工艺风险明显不一致、模型结果缺失或输入超出训练范围时，系统增加人工复核原因，不强行合并成确定结论。

### 5.4 知识检索与处置推荐

知识库包含 6 条教学规则。每条规则包含：

- 标题、来源和证据等级；
- 适用缺陷、工艺信号和风险等级；
- 检查步骤和候选处置措施；
- 适用边界和停止条件。

检索模块根据缺陷类别、工艺信号、风险和关键词计算匹配分数，支持 `scratch/划痕/擦伤`、`crack/裂纹/龟裂` 等受控中英文同义词扩展，以及证据等级、来源和最低分筛选，并返回扩展词和匹配理由。推荐模块对知识命中进行去重和整理，输出检查项、处置候选和停止条件。所有结果均设置 `humanApprovalRequired=true`。

## 6. 系统实现

### 6.1 前端 Dashboard

前端提供以下主要区域：

- 平台总览和模型服务状态；
- 图片上传、Canvas 预览和真实检测框；
- 工艺 9 特征输入、预测值、风险和范围告警；
- 多源证据、融合分数、来源一致性和复核原因；
- 知识条目、候选检查和处置建议；
- 人工确认、驳回、暂缓、动作选择和审核备注；
- 决策队列、审核历史、详情查看、状态筛选和 CSV 导出；
- 历史检测记录。

前端不保存密钥，也不直接调用外部大模型。图片以受限 Base64 data URL 发送到 Node 服务，服务端检查格式和大小后再转发到本地视觉服务。

### 6.2 Node REST API

| 接口 | 方法 | 作用 |
|---|---|---|
| `/api/health` | GET | 服务健康检查 |
| `/api/pipeline` | GET | 流水线和适配状态 |
| `/api/summary` | GET | 总览统计 |
| `/api/inspections` | GET | 查询检测记录 |
| `/api/inspect` | POST | 提交视觉检测 |
| `/api/model-status` | GET | 视觉模型状态 |
| `/api/process-quality/status` | GET | 工艺模型状态 |
| `/api/process-quality/predict` | POST | 工艺质量预测 |
| `/api/knowledge` | GET | 查询知识条目 |
| `/api/recommendations` | POST | 融合证据、生成建议并保存决策快照 |
| `/api/recommendations/review` | POST | 追加人工确认、驳回或暂缓记录 |
| `/api/recommendations/decisions[/:id]` | GET | 查询决策队列或详情 |
| `/api/recommendations/reviews[/:id]` | GET | 查询审核列表或详情 |
| `/api/recommendations/reviews/export.csv` | GET | 导出审核明细 CSV |

Node 服务负责输入校验、调用模型、整理响应、记录结果和暴露错误。它不直接执行模型训练，也不向生产设备发送控制命令。

### 6.3 Python 推理服务

两个模型服务均使用 Python 标准库 `HTTPServer` 实现轻量 HTTP 接口。视觉服务加载 YOLO 权重，完成图片解码、推理和结果转换；工艺服务加载 joblib 模型与元数据，校验 9 个特征并输出预测和范围告警。

原方案曾考虑 FastAPI。最终采用标准库服务是为了降低依赖并适应现有项目，但该实现不适合直接承担生产并发和安全要求。

### 6.4 数据记录、审核存储和一键启动

平台使用 JSON 保存检测记录，使用 `knowledge/entries.json` 保存规则知识，使用原生 `node:sqlite` 保存审核数据。`decisions` 表保存生成时的不可变推荐快照，`reviews` 表采用追加式记录，保存审核人、角色、结论、备注、选中动作和时间；决策当前状态由最近一条审核记录确定。CSV 导出使用 UTF-8 BOM，并对公式注入进行防护。默认数据库为 `data/quality-audit.sqlite`，运行文件受 Git 忽略。

`start-platform.bat` 会探测 `cv_tutorial`、项目 Python 环境或系统 Python，检查本地模型，并分别启动 8010、8000 和 4173 服务。缺少某个模型时，启动器会跳过对应服务并保留平台状态提示。Node 服务默认绑定 `127.0.0.1`，避免课程 Demo 默认暴露到局域网。

## 7. 测试与结果分析

### 7.1 YOLO11n 模型结果

| 数据集 | Precision | Recall | mAP50 | mAP50-95 |
|---|---:|---:|---:|---:|
| 验证集 | 0.685 | 0.764 | 0.765 | 0.451 |
| 独立测试集 | 0.676 | 0.692 | 0.747 | 0.402 |

验证集和测试集指标接近，未出现明显的验证集与测试集断层。`crazing` 类别相对较弱，是后续优化重点。模型导出为固定输入 320×320 的 ONNX，ONNX checker 和 ONNX Runtime CPU 推理通过。

### 7.2 工艺模型结果

| 指标 | 测试结果 |
|---|---:|
| MAE | 0.87194 |
| RMSE | 1.11653 |
| R² | 0.13100 |
| 相对中位数基线 MAE 改善 | 10.21% |

R² 为 0.13100，说明模型对未来时间段的解释能力有限。MAE 相对基线改善 10.21%，表明随机森林比简单常数预测更有效，但提升幅度不足以支持生产级质量控制。joblib 与 ONNX 在不同批次上的最大绝对误差均小于或等于 `1.47e-6`。

### 7.3 端到端联调

视觉联调使用 `crazing_271.jpg`：Node 服务调用 YOLO 服务后返回 HTTP 201、质量分 39、中风险和 2 个裂纹候选。工艺联调使用前端默认 9 特征：返回预测值 1.989%、低风险和 0 项范围告警；越界样例可返回 4 项范围告警；缺失特征返回 HTTP 400。

### 7.4 自动化测试

`npm test` 共 33 项，覆盖：

- API 健康检查、汇总、流水线和记录；
- 数据样例、Schema 和预处理索引；
- 图片类型、大小和 Base64 校验；
- 视觉远程推理和降级模式；
- 工艺 9 特征契约、范围告警和错误状态；
- 中英文知识检索、筛选、未知查询和匹配理由；
- 受控证据融合档案、冲突、无证据和人工审批；
- 推荐决策持久化、追加式复核、非法动作和驳回备注校验；
- 决策/审核查询、审核详情、CSV 导出和安全降级。

当前结果为 33/33 通过。独立 HTTP 冒烟测试验证了同义词检索、`conservative` 档案、决策初始 `pending`、人工审核后 `approved`、详情查询和 CSV 记录。浏览器自动化工具连接时仍发生运行时崩溃，因此本报告只采用已完成的 HTTP、静态页面、语法和自动化测试证据，不虚构跨设备截图结果。

## 8. AI 工具使用与 Git 过程管理

### 8.1 AI 工具使用

本项目使用 Codex 作为 AI 编程 Harness，主要模型记录为 GPT-5.6sol。AI 用于：

- 任务书分析和选题去重；
- 方案拆解和接口设计；
- 数据处理、训练和推理代码辅助；
- 前后端实现、测试生成和错误定位；
- README、模型卡、Prompt 记录和报告整理。

AI 输出均需经过人工阅读、命令运行、测试和业务含义核对。项目没有调用大语言模型自动生成生产处置指令。

### 8.2 AI 出错与修正

1. AI 曾使用 `node --test test`，导致 Node 将 `test` 识别为模块并报错。人工改为 `node --test "test/*.test.js"`；
2. 早期工艺建模倾向随机切分。人工根据时间部署场景改为时间顺序切分，得到更保守的 R²；
3. 方案中计划 FastAPI 和 Isolation Forest，实际根据时间和依赖情况采用标准库 `HTTPServer` 与范围告警，并在复盘中明确未完成项；
4. 早期 Prompt 记录不完整，后续以 `reconstructed_summary` 回溯补齐，并明确不冒充逐字原始对话。

### 8.3 Git 与安全管理

项目使用 Git 记录阶段变化，并通过 GitHub 公开代码、Markdown 文档、小型样例数据和 Prompt JSON。完整原始数据、模型权重、训练输出和 Token 由 `.gitignore` 与提交前检查排除。本次课程报告整理按要求只在本地修改和备份，不提交或上传。

## 9. 总结

本课程设计实现了一个制造质量异常辅助分析平台，将 YOLO11n 表面缺陷检测、RandomForest 工艺质量预测、历史记录、受控证据融合、规则知识库和 SQLite 人工复核组织为完整 B/S Demo。系统能够从图片和工艺参数获得模型结果，显示证据来源和风险差异，给出要求人工批准的候选处置建议，并保存确认、驳回或暂缓记录用于追溯。

项目的主要收获是理解了制造智能系统不能只追求单一模型指标，还需要考虑数据泄漏、时间外推、模型部署、接口契约、降级策略、知识来源、人工决策边界和过程审计。当前成果满足教学演示和课程技术综合应用要求，但仍需更多现场数据、生产级数据库、身份权限、正式报告和安全机制才能进一步接近生产应用。

## 10. 参考文献

1. 《制造智能技术课程设计任务书》；
2. Breiman, L. Random Forests. Machine Learning, 2001；
3. Liu, F. T., Ting, K. M., Zhou, Z.-H. Isolation Forest. ICDM, 2008；
4. Ultralytics YOLO Documentation；
5. scikit-learn User Guide: Ensemble Methods；
6. NEU Surface Defect Database 数据说明；
7. Quality Prediction in a Mining Process 数据集说明；
8. Git Documentation；
9. GitHub Docs。

