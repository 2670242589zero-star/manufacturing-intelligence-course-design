# 阶段 6.1 自查

自查日期：2026-08-30

## 1. 数据与预处理

- [x] 使用公开数据集 `Quality Prediction in a Mining Process`，来源、许可和引用已写入 README 与数据卡。
- [x] 完整 CSV 共 737453 条、24 列，SHA-256 已核验；完整数据保留在 `data/local/`，不上传 GitHub。
- [x] 已实现逗号小数解析、时间戳解析、小时聚合、缺失值审计和时间顺序切分。
- [x] 7 个浮选柱气流/液位字段分别聚合为平均特征。
- [x] 默认排除后验质量字段 `iron_concentrate_pct`，避免明显目标泄漏。

## 2. 模型训练与导出

- [x] 已比较三组 RandomForestRegressor 参数，以验证集 MAE 选择模型。
- [x] 训练/验证/测试按时间顺序切分为 2867/615/615。
- [x] 测试 MAE `0.87194`、RMSE `1.11653`、R² `0.13100`。
- [x] 相对中位数基线 MAE 改善 `10.21%`。
- [x] 已导出 joblib、ONNX opset 17 和模型清单。
- [x] joblib 与 ONNX 的 128 条独立输入最大绝对差为 `1.47e-6`。
- [x] 报告明确说明当前模型是可复现课程基线，不宣称生产现场高精度。

## 3. 推理与平台接入

- [x] 已创建 `inference/process_quality_service.py`，提供 `GET /health` 和 `POST /predict`。
- [x] 已创建 Node HTTP 适配器和输入校验。
- [x] 已新增 `GET /api/process-quality/status` 与 `POST /api/process-quality/predict`。
- [x] 模型未配置或服务不可用时返回 `503`，不生成伪预测。
- [x] Dashboard 已新增工艺预测页面，可展示预测值、风险、测试指标、特征重要性和范围告警。
- [x] 已使用前端默认 9 特征完成 Node 到 Python 端到端联调，返回 `1.989%` 和低风险。

## 4. 测试、安全与追溯

- [x] Node 自动化测试 `23/23` 通过，覆盖特征校验、适配器状态、远程预测、不可用状态和 API 转发。
- [x] Node/前端 JavaScript 语法检查和 Python `py_compile` 均通过。
- [x] 模型二进制、完整数据、小时聚合文件、运行日志和本地备份均不进入公开仓库。
- [x] 10 份 Prompt JSON 与训练报告均可解析；阶段记录不包含 Token、Cookie、密码或完整原始数据。
- [x] 已创建 `_backups/step-13-process-model/final`，28 个关键文件的源/备份 SHA-256 逐项一致，并登记 6 个本地大产物哈希。
- [ ] 浏览器视觉自动化未完成：Codex 浏览器运行时连续返回 `windows sandbox failed: helper_unknown_error: setup refresh had errors`；已完成 HTTP、代码结构和响应式 CSS 静态验收，需环境恢复后补截图验证。
- [x] 本阶段按用户“先不提交”的要求保持未提交、未推送状态。
